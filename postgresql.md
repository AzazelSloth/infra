# Installation

Ubuntu includes PostgreSQL by default. To install PostgreSQL on Ubuntu, use the apt (or other apt-driving) command:

```bash
apt install postgresql
```

## Database creation

To create a new database from the command line, use the following command:

```bash
sudo -u postgres createdb demo
```
